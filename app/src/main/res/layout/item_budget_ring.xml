<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="120dp"
    android:layout_height="wrap_content"
    android:layout_marginEnd="12dp"
    android:background="@drawable/card_glass_budget_ring"
    android:gravity="center_horizontal"
    android:orientation="vertical"
    android:padding="14dp">

    <!-- Circular Progress Ring -->
    <FrameLayout
        android:layout_width="64dp"
        android:layout_height="64dp"
        android:layout_gravity="center">

        <ProgressBar
            android:id="@+id/pbRing"
            style="?android:attr/progressBarStyleHorizontal"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:indeterminate="false"
            android:max="100"
            android:progress="75"
            android:progressDrawable="@drawable/progress_ring_bg"
            android:rotation="-90" />

        <!-- Percentage Text in Center -->
        <TextView
            android:id="@+id/tvRingPercent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:text="75%"
            android:textColor="?attr/themeTextPrimary"
            android:textSize="13sp"
            android:textStyle="bold" />
    </FrameLayout>

    <!-- Category Name -->
    <TextView
        android:id="@+id/tvRingCategory"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:text="Food"
        android:textColor="?attr/themeTextPrimary"
        android:textSize="12sp"
        android:textStyle="bold" />

    <!-- Spent / Budget -->
    <TextView
        android:id="@+id/tvRingAmount"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="2dp"
        android:gravity="center"
        android:text="₹4,500 / ₹6,000"
        android:textColor="?attr/themeTextSecondary"
        android:textSize="10sp" />
</LinearLayout>
